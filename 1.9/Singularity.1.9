BootStrap: docker
From: continuumio/miniconda3:4.7.12

%apprun samtools
  exec samtools "${@}"

%post
  . /opt/conda/bin/activate && \
  conda install -q -y \
    -c bioconda samtools=1.9 && \
  conda clean -a -q -f -y
